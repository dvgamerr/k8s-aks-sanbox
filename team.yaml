image:
  repository: kubernetesui/dashboard

protocolHttp: true

# ingress:
#   enabled: false
#   paths:
#     - /aks-team-ranger
#   hosts:
#     - sanbox.aks-team.touno.io
#   tls:
#     - secretName: sanbox.aks-team
#       hosts:
#         - sanbox.aks-team.touno.io

#   annotations:
#     kubernetes.io/ingress.class: dashboard
#     cert-manager.io/cluster-issuer: letsencrypt
  
extraArgs:
  - --namespace=aks-team-ranger
  - --enable-skip-login

metricsScraper:
  enabled: true

rbac:
  create: false
  clusterRoleMetrics: false

serviceAccount:
  create: false
  name: team-aks-team-ranger

settings:
  clusterName: "AKS Team Ranger"
  defaultNamespace: "aks-team-ranger"
  itemsPerPage: 20
  resourceAutoRefreshTimeInterval: 5
  disableAccessDeniedNotifications: true
  namespaceFallbackList:
    - aks-team-ranger